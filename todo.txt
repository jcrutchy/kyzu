## 🔴 High Priority: Precision & Math
* [ ] **Vertex Radius Scaling:** Ensure `BakedVertex` (currently a unit sphere $[-1, 1]$) is correctly scaled by the body's radius.
    * *Note:* Next step is to multiply `model_mat` by the planet's radius ($6,371,000.0$) in `SolidModule::update`.
* [ ] Refactor Mesh Topology: Move from Indexed to Non-Indexed triangles in the BakeManager.
* [ ] Add Barycentric Attributes: Include f32x3 edge-distance data in BakedVertex.
    * *Note:* Why: Allows for high-performance, scale-invariant wireframes and "hex-grid" overlays without expensive fragment derivatives.

## 🟡 Medium Priority: Refactoring & Safety
* [ ] **Logger Performance:** Move file I/O to a buffered approach or a background thread.
* [ ] **Bake Handshake:** Add a check in `App::resumed` to verify `.bake` files exist (prevents `expect` panics).
* [ ] **Floating Origin Verification:** Run a "long-distance test." Set altitude to $150,000,000,000.0$ (1 AU) and ensure the icosahedron still renders smoothly when zoomed in.
