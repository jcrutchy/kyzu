### `TODO.md`

## 🔴 High Priority: Precision & Math
* [ ] **The "Eye-Relative" Transition:** Update `SharedState` and `CameraGpu` to store `eye_world` as `f64`.
    * *Why:* At Earth scale ($6.37 \times 10^6$ meters), `f32` jitter will cause vertices to "shake" as you move.
    * *Fix:* Pass `f64` eye to GPU; subtract `f64` eye from `f64` world-pos *before* casting to `f32` for the Projection matrix.
* [ ] **Vertex Radius Scaling:** Ensure `BakedVertex` (which is currently a unit sphere $[-1, 1]$) is correctly scaled by the body's radius in the shader or the `PlanetModule`.
* [ ] **Orbital Controller Precision:** Move `lat`, `lon`, and `altitude` in `orbital.rs` from `f32` to `f64`.

## 🟡 Medium Priority: Refactoring & Safety
* [ ] **Logger Performance:** Move file I/O to a buffered approach or a background thread.
    * *Current:* Opens/closes `kyzu.log` on every `emit`. Fine for bakes, but will stutter during 60fps frame logging.
* [ ] **Bake Handshake:** Add a check in `App::resumed` to verify the `.bake` file exists and is "fresh" before the `RenderModule` tries to load it (prevents `expect` panics).

## 🟢 Low Priority: Cleanup & Housekeeping
* [ ] **Delete Orphaned Files:**
    * [ ] `render/camera/transition.rs` (Old duplicate of `orbital.rs`).
    * [ ] `render/geometry.rs` (Hardcoded icosahedron superseded by bake pipeline).
* [ ] **UI Sync:** Verify `egui_wgpu` initialization in `ui/mod.rs` matches current crate version APIs (RendererOptions).
* [ ] **Documentation:** Add doc comments to `BakedVertex` to clarify that `pos` is "Unit Sphere Space" and not "World Space."
